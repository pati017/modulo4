import { Router } from 'express';

const router = new Router();

//Routes

router.get('/');

router.post('/');

router.put('/:id');

router.delete('/:id');

router.get('/:id');

export default router;